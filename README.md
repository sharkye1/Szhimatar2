# 🎬 Szhimatar Video Compressor

Современный оффлайн-видеокомпрессор на базе Tauri + React + TypeScript.

## 🏗️ Архитектура проекта

доступна в [PROJECT_STRUCTURE.md](PROJECT_STRUCTURE.md)


## 🚀 Запуск проекта

### Требования

- **Node.js** 18+ и npm
- **Rust** 1.70+ (установить через [rustup](https://rustup.rs/))
- **Windows**: Visual Studio Build Tools или Visual Studio с C++ workload
- **FFmpeg** (для работы headless-стенда и будущей интеграции) — должен быть доступен в PATH

### Установка зависимостей

```bash
# Установить npm зависимости
npm install

# Rust зависимости установятся автоматически при первой сборке
```

### Режим разработки

```bash
npm run tauri dev
```

### 🧪 Тестирование UI с фейковым оборудованием

Для тестирования визуального отображения под разные CPU/GPU без реального оборудования:

```bash
# Скопировать пример конфига
Copy-Item .hardware-override.example.json .hardware-override.json

# Отредактировать .hardware-override.json (enabled: true, изменить vendor)
# Запустить приложение
npm run tauri dev
```

**Влияет только на UI** (градиенты кнопок, badges). FFmpeg рендеринг использует реальное оборудование.  
Подробнее: [HARDWARE_OVERRIDE_QUICKSTART.md](HARDWARE_OVERRIDE_QUICKSTART.md)

## 🧪 FFmpeg тестовый стенд (без UI)

В репозитории есть отдельный инструмент для массового тестирования параметров сжатия без запуска UI: см. [test-compressor/README.md](test-compressor/README.md).

Быстрый запуск на коротком видео:

```bash
cd test-compressor
npm install
# Сгенерировать 3-сек тестовый файл
ffmpeg -f lavfi -i testsrc=duration=3:size=1920x1080:rate=30 -pix_fmt yuv420p test-video/input.mp4
# Запуск всех комбинаций и генерация отчётов (JSON/TXT/CSV в output/)
npm start
```

Этот стенд поддерживает слоты CPU/GPU, безопасное завершение, уникальные имена файлов и устойчив к зависаниям.

## 🎨 Особенности

### ✅ Реализовано

- **Архитектура окон**: Главное окно + 4 окна настроек (видео, аудио, общие, watermark)
- **Темы**: 7 тем (светлая + 6 тёмных: blue, green, orange, red, purple-pink, blue-ocean)
- **Языки**: 7 языков (русский, английский, китайский, эсперанто, межславянский, взрывной стиль, пустой шаблон)
- **UI компоненты**: 
  - Выбор файлов и папок
  - Очередь обработки с прогресс-барами и ETA
  - Кнопки управления (Start, Pause, Stop, Clear)
  - Формы настроек для всех параметров
  - **RenderModeSelector** - визуальный селектор CPU/GPU/Duo с аппаратно-зависимыми градиентами
  - **StatisticsPanel** - панель статистики с графиками и детальными метриками
  - **PresetManager** - управление пользовательскими пресетами
  - **FfmpegManager** - управление FFmpeg путями и версиями
- **Система логирования**: Запись действий в `~/.szhimatar/logs/app.log`
- **Система настроек**: Сохранение в `~/.szhimatar/settings.json`
- **Анимации**: Fade-in, slide-in, hover эффекты, пульсации при рендере
- **FFmpeg конфигурация**: Автоматическое обнаружение, ручная настройка путей, очистка Windows-префиксов
- **GPU поддержка**: Проверка NVENC совместимости с валидацией путей
- **🎨 Hardware Detection**: Автоопределение CPU/GPU вендоров (Intel/AMD/NVIDIA) с динамическими градиентами
- **🆕 Duo Mode**: Параллельный рендеринг на CPU и GPU одновременно с RenderScheduler — см. [DUO_MODE_GUIDE.md](DUO_MODE_GUIDE.md)
- **📊 Статистика**: Агрегация результатов рендера, сохранение в JSON, визуализация метрик — см. [STATISTICS_GUIDE.md](STATISTICS_GUIDE.md)
- **🧪 Hardware Override**: Система подмены параметров оборудования для тестирования UI — см. [HARDWARE_OVERRIDE_QUICKSTART.md](HARDWARE_OVERRIDE_QUICKSTART.md)

### ⏳ В процессе / планируется

- Предпросмотр видео (1-2 кадра)
- Экспорт статистики в CSV/Excel
- Batch операции (массовая остановка, изменение приоритетов)
- Проверка обновлений через GitHub API
- Интеграция с облачными хранилищами

## 📁 Хранилище данных

Все данные приложения хранятся локально:

- **Windows**: `C:\Users\<username>\.szhimatar\`
- **Linux/Mac (будет реализовано позже)**: `~/.szhimatar/`

Структура:
```
.szhimatar/
├── logs/
│   └── app.log           # Логи действий
├── stats/
│   └── stat.json         # Статистика (будущее)
└── settings.json         # Настройки пользователя
```

## 🛠️ Технологии

- **Frontend**: React 18 + TypeScript
- **Backend**: Rust (Tauri 1.5)
- **Сборка**: Vite 5
- **Стилизация**: Vanilla CSS с CSS Variables для тем
- **Роутинг**: React Router 6

## 🎯 Принципы разработки

1. **Полностью оффлайн** - никаких сетевых запросов для работы программы
2. **Минимальный вес** - обеспечивает Tauri
3. **Модульность** - легко добавлять новые темы, языки, функции
4. **Читаемость** - чистый код `😅` без хаков
5. **Расширяемость** - готово к будущим улучшениям

## 📝 Лицензия

MIT
Подробнее в файле [LICENSE](LICENSE).